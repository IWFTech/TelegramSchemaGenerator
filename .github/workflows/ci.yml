name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  DOTNET_NOLOGO: "1"
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

jobs:
  verify:
    name: Verify (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os:
          - windows-latest
          - ubuntu-latest
          - macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        shell: pwsh
        run: dotnet restore ./TeleFlow.Telegram.SchemaGenerator.csproj

      - name: Restore tests
        shell: pwsh
        run: dotnet restore ./tests/TeleFlow.Telegram.SchemaGenerator.Tests/TeleFlow.Telegram.SchemaGenerator.Tests.csproj

      - name: Verify whitespace formatting
        shell: pwsh
        run: dotnet format whitespace ./TeleFlow.Telegram.SchemaGenerator.csproj --verify-no-changes --no-restore --verbosity minimal

      - name: Verify test whitespace formatting
        shell: pwsh
        run: dotnet format whitespace ./tests/TeleFlow.Telegram.SchemaGenerator.Tests/TeleFlow.Telegram.SchemaGenerator.Tests.csproj --verify-no-changes --no-restore --verbosity minimal

      - name: Verify style formatting
        shell: pwsh
        run: dotnet format style ./TeleFlow.Telegram.SchemaGenerator.csproj --verify-no-changes --no-restore --verbosity minimal

      - name: Verify test style formatting
        shell: pwsh
        run: dotnet format style ./tests/TeleFlow.Telegram.SchemaGenerator.Tests/TeleFlow.Telegram.SchemaGenerator.Tests.csproj --verify-no-changes --no-restore --verbosity minimal

      - name: Build
        shell: pwsh
        run: dotnet build ./TeleFlow.Telegram.SchemaGenerator.csproj -c Release --no-restore /nodeReuse:false

      - name: Test
        shell: pwsh
        run: dotnet test ./tests/TeleFlow.Telegram.SchemaGenerator.Tests/TeleFlow.Telegram.SchemaGenerator.Tests.csproj -c Release --no-restore /nodeReuse:false --logger "console;verbosity=minimal"
