name: Telegram Bot API Monitor

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
    inputs:
      force_regenerate:
        description: "Create a refresh PR when Telegram Bot API version did not change (does not modify an open update PR)"
        type: boolean
        required: false
        default: false

permissions:
  contents: read
  pull-requests: read
  issues: write

concurrency:
  group: telegram-api-monitor
  cancel-in-progress: false

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"
  DOTNET_NOLOGO: "1"
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages
  TELEFLOW_UPDATE_TOKEN: ${{ secrets.TELEFLOW_UPDATE_TOKEN }}

jobs:
  monitor:
    name: Check Telegram Bot API
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout generator
        uses: actions/checkout@v7
        with:
          path: generator

      - name: Checkout TeleFlow
        uses: actions/checkout@v7
        with:
          repository: IWFTech/TeleFlow
          ref: main
          path: teleflow
          token: ${{ env.TELEFLOW_UPDATE_TOKEN || github.token }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Check latest Telegram Bot API
        id: check
        shell: pwsh
        working-directory: generator
        run: |
          ./eng/check-telegram-bot-api.ps1 `
            -TeleFlowRoot ../teleflow `
            -OutputPath ../telegram-bot-api-check.json `
            -FailureOutputPath ../telegram-api-monitor-artifacts

      - name: Publish check summary
        shell: pwsh
        run: |
          $result = Get-Content -Raw -LiteralPath telegram-bot-api-check.json | ConvertFrom-Json
          $forceRegenerate = "${{ github.event_name == 'workflow_dispatch' && inputs.force_regenerate == true }}"
          if ($result.Status -eq "configuration-required") {
            @"
          ## Telegram Bot API monitor

          Schema processing stopped and requires a configuration decision.
          Message: $($result.Diagnostics.Message)
          "@ | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
            exit 0
          }

          @"
          ## Telegram Bot API monitor

          Current TeleFlow version: $($result.Current.Version)
          Latest Telegram version: $($result.Latest.Version)
          Latest release date: $($result.Latest.ReleaseDate)
          Update available: $($result.HasUpdate)
          Force regenerate: $forceRegenerate
          "@ | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append

      - name: Upload schema diagnostics
        if: ${{ steps.check.outputs.schema_error == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: telegram-schema-diagnostics-${{ github.run_id }}
          path: telegram-api-monitor-artifacts
          if-no-files-found: error
          retention-days: 90

      - name: Create schema configuration issue
        if: ${{ steps.check.outputs.schema_error == 'true' }}
        uses: actions/github-script@v7
        env:
          CHECK_PATH: telegram-bot-api-check.json
        with:
          script: |
            const fs = require('fs');
            const check = JSON.parse(fs.readFileSync(process.env.CHECK_PATH, 'utf8'));
            const title = '[Telegram Schema] Configuration decision required';
            const { owner, repo } = context.repo;
            const openIssues = await github.paginate(github.rest.issues.listForRepo, {
              owner,
              repo,
              state: 'open',
              per_page: 100
            });
            const existingIssue = openIssues.find(issue => issue.title === title);
            if (existingIssue) {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: existingIssue.number,
                body: `Another schema configuration failure was detected in workflow run ${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}.\n\nMessage: ${check.Diagnostics.Message}`
              });
              return;
            }

            const diagnostics = check.Diagnostics;
            const body = [
              '## Summary',
              'Telegram Bot API schema processing stopped because a semantic configuration decision is required.',
              '',
              `Message: ${diagnostics.Message}`,
              `Source URL: ${diagnostics.SourceUrl}`,
              `Configuration: ${diagnostics.GeneratorConfiguration}`,
              '',
              `Workflow run: ${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`,
              '',
              'The raw snapshot and diagnostics are available in the workflow artifacts. Add or adjust the typed generator override, then rerun the monitor.'
            ].join('\n');
            await github.rest.issues.create({ owner, repo, title, body });

      - name: Resolve generation plan
        id: plan
        shell: pwsh
        run: |
          $hasUpdate = "${{ steps.check.outputs.has_update }}" -eq "true"
          $schemaError = "${{ steps.check.outputs.schema_error }}" -eq "true"
          $hasVersionUpdate = "${{ steps.check.outputs.has_version_update }}" -eq "true"
          $hasSemanticUpdate = "${{ steps.check.outputs.has_semantic_update }}" -eq "true"
          $forceRegenerate = "${{ github.event_name == 'workflow_dispatch' && inputs.force_regenerate == true }}" -eq "true"
          $shouldGenerate = -not $schemaError -and ($hasUpdate -or $forceRegenerate)
          $currentVersion = "${{ steps.check.outputs.current_version }}"
          $latestVersion = "${{ steps.check.outputs.latest_version }}"
          $latestReleaseDate = "${{ steps.check.outputs.latest_release_date }}"
          $latestChangelogAnchor = "${{ steps.check.outputs.latest_changelog_anchor }}"

          if ($hasVersionUpdate) {
            $branchPrefix = "schema/update-telegram-bot-api-$latestVersion"
            $branch = "$branchPrefix-${{ github.run_id }}-${{ github.run_attempt }}"
            $commitMessage = "schema: update Telegram Bot API to $latestVersion"
            $title = "schema: update Telegram Bot API to $latestVersion"
            $summary = @"
          - update generated Telegram schema and client output
          - update Telegram Bot API badge metadata
          - Telegram Bot API: $currentVersion -> $latestVersion
          - release date: $latestReleaseDate
          - changelog anchor: $latestChangelogAnchor
          "@
          }
          elseif ($hasSemanticUpdate -or $forceRegenerate) {
            $branchPrefix = "schema/refresh-generated-output"
            $branch = "$branchPrefix-${{ github.run_id }}-${{ github.run_attempt }}"
            $commitMessage = "chore(schema): refresh generated Telegram output"
            $title = "chore(schema): refresh generated Telegram output"
            $summary = @"
          - refresh generated Telegram schema and client output with the current generator
          - update Telegram Bot API badge metadata when generated metadata changes
          - Telegram Bot API version remains $currentVersion
          "@
          }
          else {
            $branch = ""
            $branchPrefix = ""
            $commitMessage = ""
            $title = ""
            $summary = "- no generated output refresh is required"
          }

          "should_generate=$($shouldGenerate.ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "schema_error=$($schemaError.ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "has_version_update=$($hasVersionUpdate.ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "has_semantic_update=$($hasSemanticUpdate.ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "branch=$branch" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "branch_prefix=$branchPrefix" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "commit_message=$commitMessage" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "title=$title" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

          @"
          pr_body<<PR_BODY
          ## Summary
          $summary

          ## Validation
          - generated with IWFTech/TelegramSchemaGenerator
          - ran dotnet build ./TeleFlow.sln -c Release
          - ran dotnet test ./TeleFlow.sln -c Release
          - ran ./eng/verify-schema-update-guardrails.ps1 -BaseRef main -SkipFetch
          PR_BODY
          "@ | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      - name: Detect an open TeleFlow update pull request
        id: existing-update-pr
        if: ${{ steps.plan.outputs.should_generate == 'true' }}
        shell: pwsh
        env:
          TELEFLOW_UPDATE_BRANCH_PREFIX: ${{ steps.plan.outputs.branch_prefix }}
        run: |
          . (Join-Path $env:GITHUB_WORKSPACE "generator/eng/telegram-api-monitor-support.ps1")

          $headers = @{
            Accept = "application/vnd.github+json"
            "User-Agent" = "TeleFlow-Schema-Monitor"
          }

          if (-not [string]::IsNullOrWhiteSpace($env:TELEFLOW_UPDATE_TOKEN)) {
            $headers.Authorization = "Bearer $env:TELEFLOW_UPDATE_TOKEN"
          }

          $uri = "https://api.github.com/repos/IWFTech/TeleFlow/pulls?state=open&base=main&per_page=100"
          $pullRequests = Invoke-RestMethod -Uri $uri -Headers $headers
          $existingPullRequest = Find-OpenTeleFlowSchemaPullRequest `
            -PullRequests $pullRequests `
            -RepositoryFullName "IWFTech/TeleFlow" `
            -BranchPrefix $env:TELEFLOW_UPDATE_BRANCH_PREFIX
          $exists = $null -ne $existingPullRequest

          "exists=$($exists.ToString().ToLowerInvariant())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          if ($exists) {
            "url=$($existingPullRequest.html_url)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
            Write-Host "A matching TeleFlow update pull request is already open: $($existingPullRequest.html_url)"
          }

      - name: Report schema configuration requirement
        if: ${{ steps.check.outputs.schema_error == 'true' }}
        shell: pwsh
        run: Write-Host "Schema processing requires a configuration decision. See the issue and uploaded diagnostics."

      - name: Stop when no update, refresh, or new pull request is required
        if: ${{ steps.check.outputs.schema_error != 'true' && (steps.plan.outputs.should_generate != 'true' || steps.existing-update-pr.outputs.exists == 'true') }}
        shell: pwsh
        run: |
          if ("${{ steps.existing-update-pr.outputs.exists }}" -eq "true") {
            Write-Host "A matching TeleFlow update pull request is already open: ${{ steps.existing-update-pr.outputs.url }}"
            exit 0
          }

          Write-Host "TeleFlow already targets Telegram Bot API ${{ steps.check.outputs.current_version }}."

      - name: Fail clearly when update token is missing
        if: ${{ steps.plan.outputs.should_generate == 'true' && steps.existing-update-pr.outputs.exists != 'true' && env.TELEFLOW_UPDATE_TOKEN == '' }}
        shell: pwsh
        run: |
          throw "TeleFlow generated output refresh is required, but TELEFLOW_UPDATE_TOKEN is not configured."

      - name: Generate TeleFlow schema update
        if: ${{ steps.plan.outputs.should_generate == 'true' && steps.existing-update-pr.outputs.exists != 'true' && env.TELEFLOW_UPDATE_TOKEN != '' }}
        shell: pwsh
        working-directory: generator
        run: ./eng/update-teleflow-schema.ps1 -TeleFlowRoot ../teleflow

      - name: Build TeleFlow after generation
        if: ${{ steps.plan.outputs.should_generate == 'true' && steps.existing-update-pr.outputs.exists != 'true' && env.TELEFLOW_UPDATE_TOKEN != '' }}
        shell: pwsh
        working-directory: teleflow
        run: dotnet build ./TeleFlow.sln -c Release /nodeReuse:false

      - name: Test TeleFlow after generation
        if: ${{ steps.plan.outputs.should_generate == 'true' && steps.existing-update-pr.outputs.exists != 'true' && env.TELEFLOW_UPDATE_TOKEN != '' }}
        shell: pwsh
        working-directory: teleflow
        run: dotnet test ./TeleFlow.sln -c Release --no-build /nodeReuse:false --logger "console;verbosity=minimal"

      - name: Verify TeleFlow schema update guardrails
        if: ${{ steps.plan.outputs.should_generate == 'true' && steps.existing-update-pr.outputs.exists != 'true' && env.TELEFLOW_UPDATE_TOKEN != '' }}
        shell: pwsh
        working-directory: teleflow
        run: ./eng/verify-schema-update-guardrails.ps1 -BaseRef main -SkipFetch

      - name: Create TeleFlow update PR
        if: ${{ steps.plan.outputs.should_generate == 'true' && steps.existing-update-pr.outputs.exists != 'true' && env.TELEFLOW_UPDATE_TOKEN != '' }}
        uses: peter-evans/create-pull-request@v8
        with:
          path: teleflow
          token: ${{ env.TELEFLOW_UPDATE_TOKEN }}
          branch: ${{ steps.plan.outputs.branch }}
          commit-message: ${{ steps.plan.outputs.commit_message }}
          title: ${{ steps.plan.outputs.title }}
          body: ${{ steps.plan.outputs.pr_body }}
          labels: |
            area:schema
            priority:p1
